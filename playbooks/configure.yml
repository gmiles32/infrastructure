- hosts: "{{ host }}"
  vars_files:
    - ../vars/vault.yml
  roles:
    - role: grog.package
      tags: basic
    - role: geerlingguy.pip
      tags: basic
    - role: geerlingguy.docker
      tags: docker
    - role: docker_firewalld
      tags: docker
    - role: docker_compose_generator
      tags: compose