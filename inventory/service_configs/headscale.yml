compose_scheme_version: '3'
containers: 
  - service_name: headscale
    active: true
    image: headscale/headscale:latest@sha256:cd37b30018575fd62f18419138d4e526bcf894e1d4e1aa1b08699cec1891ac23
    container_name: headscale
    volumes:
<<<<<<< HEAD
      - ./headscale/config:/etc/headscale
      - ./headscale/data:/var/lib/headscale
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.headscale-rtr.rule=PathPrefix(`/`) && Host(`hs.{{ hs_domain }}`)"
      - "traefik.http.services.headscale-svc.loadbalancer.server.port=8080"
      - "traefik.docker.network=frontend"
  - service_name: traefik
    active: true
    image: traefik:v3.6.2@sha256:c025135278d10f0fb6e54cb2b2dbadb3c3f0381a2c508cd74c06a74cb5a0e828
    container_name: traefik
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - frontend
    ports:
      - 80:80
      - 443:443
    environment:
      - "CF_API_EMAIL={{ cf_api_email }}"
      - "CF_API_KEY={{ cf_api_key }}"
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik/traefik.yml:/traefik.yml:ro
      - ./traefik/acme.json:/acme.json

  - service_name: authelia
    active: true
    image: authelia/authelia:4.39.14@sha256:88f1494b6ac1174641770f106335ab67752d66e5822b4059badca220b5d6153b
    container_name: authelia
    user: 1000:1000
    volumes:
      - ./authelia:/config
    networks:
      - frontend
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.authelia.rule=Host(`auth.{{ hs_domain }}`)'
      - 'traefik.http.routers.authelia.entrypoints=websecure'
      - 'traefik.http.routers.authelia.tls=true'
      - "traefik.http.routers.authelia-secure.tls.certresolver=cloudflare"
      - "traefik.http.routers.authelia-secure.tls.domains[0].main=auth.{{ hs_domain }}"
      - 'traefik.http.middlewares.authelia.forwardAuth.address=http://authelia:9091/api/verify?rd=https%3A%2F%2Fauth.{{ hs_domain }}%2F'
      - 'traefik.http.middlewares.authelia.forwardauth.trustForwardHeader=true'
      - 'traefik.http.middlewares.authelia.forwardauth.authResponseHeaders=Remote-User,Remote-Groups,Remote-Name,Remote-Email'  # yamllint disable-line rule:line-length
      - "traefik.docker.network=frontend"
=======
      - ./config:/etc/headscale
      - ./data:/var/lib/headscale
    ports:
      - 8080:8080
    command: headscale serve
>>>>>>> 96a130f (Updated resources for headscale)
    restart: unless-stopped

  - service_name: headscale-ui
    active: true
    image: ghcr.io/gurucomputing/headscale-ui:latest@sha256:d86943ae41059367f930c9794f7eddaf321823336184a3b8e1a08e9a4baac870
    container_name: headscale-ui
    ports:
      - 9443:443

networks: []

volumes: []